# AlphaCore — 生产级框架优化与智能决策信号预案

> 编制日期：2026-05-06  
> 当前版本：v3.0（11 条路由、8 个前端面板、Tushare+FRED 双源架构）

---

## 一、全系统审计：当前短板清单

逐面板审视后，以下问题阻碍了"辅助决策"的核心目标：

| 层级 | 问题 | 严重度 |
|:---|:---|:---:|
| **决策** | 11 个数据面板各自独立，用户看完需要自己合成为操作信号 | 🔴 |
| **决策** | 没有统一的"当前该买还是该卖"的量化评分 | 🔴 |
| **框架** | Hero 区域是静态营销文案，浪费了页面最黄金的首屏位置 | 🔴 |
| **框架** | 9 个 glass-card 垂直堆叠，页面过长，无折叠/分组 | 🟡 |
| **框架** | main.js 已膨胀至 750+ 行，所有逻辑耦合在一个文件 | 🟡 |
| **警示** | 告警系统只在极端事件触发 Server酱推送，前端不可见 | 🔴 |
| **警示** | 利率倒挂时没有在其他面板（资产配置/回测）中显示警告标记 | 🟡 |
| **视觉** | 各面板样式靠内联 style 驱动，缺乏统一的设计 token | 🟡 |
| **视觉** | 没有统一的"当前状态摘要"仪表盘 | 🔴 |

---

## 二、核心方案：三件套架构

```
┌─────────────────────────────────────────────────┐
│  🔴🟡🟢 量化决策仪表盘 (首屏 Hero → Live Dashboard)  │  ← 新增
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 综合信号  │ │ 当前象限  │ │ 建议仓位 股/债/金/现 │ │
│  │ 强烈买入  │ │ 通胀恐慌  │ │  0% / 0% / 40% / 60%│ │
│  │ 得分: 82  │ │ ⚠️ 利率倒挂│ │  ⚠️ 建议增持现金    │ │
│  └──────────┘ └──────────┘ └──────────────────┘ │
├─────────────────────────────────────────────────┤
│  ⚠️ 活跃警示 (2)                                 │  ← 新增
│  ▸ 2s10s 利差深度倒挂 -65bp（已持续 487 天）      │
│  ▸ SPY-TLT 相关性 +0.42 — 股债双杀风险            │
├─────────────────────────────────────────────────┤
│  📊 数据面板（折叠式分组）                         │  ← 重构
│  ▸ 宏观雷达 [TNX ▾] [VIX ▾] [利率曲线 ▾]         │
│  ▸ 风险分析 [相关性 ▸] [蒙特卡洛 ▸] [压力测试 ▸]  │
│  ▸ 组合优化 [配置沙盘 ▸] [有效前沿 ▸] [回测 ▸]    │
│  ▸ AI 决策 [CIO 洞察 ▸]                          │
└─────────────────────────────────────────────────┘
```

---

## 三、阶段拆解

### Phase 1：综合买入/卖出信号引擎

**新增：** `core/decision_signal.py` + `GET /api/macro/decision`

**评分模型（5 因子加权，得分 0-100，映射到 5 级信号）：**

| 因子 | 权重 | 极度看空(0分) | 看空(25分) | 中性(50分) | 看多(75分) | 极度看多(100分) |
|:---|:---:|:---|:---|:---|:---|:---|
| VIX 位势 | 25% | >30 | 20-30 | 15-20 | 12-15 | <12 |
| 利率方向 | 20% | TNX>4.5 + 上行 | TNX>4.5 下行 | 3.8-4.5 | <3.8 下行 | <3.8 快速下行 |
| 曲线形态 | 20% | 深度倒挂< -50bp | 倒挂<0 | 平坦 0-50bp | 陡峭 50-100bp | 极度陡峭>100bp |
| 股债相关 | 20% | 双杀 >0.3 | 弱正 0-0.3 | 中性 -0.2~0 | 健康 -0.5~-0.2 | 极健康 <-0.5 |
| 当前象限 | 15% | 流动性枯竭 | 通胀型恐慌 | 通缩型恐慌 | 中性震荡 | 常态扩张 |

**输出格式：**

```json
{
  "score": 82,
  "signal": "强烈买入",
  "signal_en": "STRONG_BUY",
  "color": "#4ade80",
  "factors": [
    {"name": "VIX 位势", "score": 75, "detail": "VIX 14.2，低波动安全区"},
    {"name": "利率方向", "score": 75, "detail": "TNX 3.6%，宽松周期"},
    ...
  ],
  "regime": {"name": "常态扩张期", "color": "#4ade80"},
  "allocation": {"spy": 60, "tlt": 30, "gld": 10, "cash": 0},
  "active_warnings": [
    {"level": "warning", "text": "2s10s 利差倒挂 -65bp", "source": "yield_curve"},
    {"level": "danger", "text": "SPY-TLT 相关性 +0.42 股债双杀风险", "source": "correlation"}
  ]
}
```

### Phase 2：首屏 Hero → 实时仪表盘

**改造 `index.html` Hero 区域：**

- 移除静态文案，替换为 `id="dashboard"` 的动态面板
- 三个仪表卡片横向排列：综合信号（大号圆环仪表盘）、当前象限（带颜色标签）、建议仓位（四通道条形图）
- 下方一条警示横幅（如果有活跃警告则红色闪烁，无警告则绿色"系统正常"）
- 数据来源：调用 `/api/macro/decision`

**前端新增：** `initDashboard()` 函数，DOMContentLoaded 时最先调用。

### Phase 3：面板分组折叠

**改造面板区域：** 将 9 个 glass-card 归入 4 个可折叠的 section：

| 分组 | 面板 | 默认状态 |
|:---|:---|:---:|
| 📡 宏观雷达 | TNX, VIX, 利率曲线 | 展开 |
| ⚠️ 风险分析 | 相关性, 蒙特卡洛, 压力测试 | 展开 |
| 📐 组合优化 | 配置沙盘, 有效前沿, 回测 | 折叠 |
| 🤖 AI 决策 | CIO 洞察 | 展开 |

每个 section 使用 `<details open>` 标签或 JS 折叠组件，点击标题展开/折叠。减少首屏滚动距离。

### Phase 4：跨面板警示联动

**后端新增：** `core/alert_state.py` — 全局警示状态（模块级 dict），各引擎在计算时写入警示。

```python
# 各引擎中（quant_engine, yield_curve, scenario, backtest）：
from core.alert_state import set_alert, clear_alert
set_alert("correlation", "danger", "SPY-TLT 股债双杀 +0.42")
set_alert("yield_curve", "warning", "2s10s 利差倒挂 -65bp, 连续 487 天")
```

**前端轮询：** `/api/macro/decision` 返回 `active_warnings` 数组，前端 Dashboard 中的警示横幅实时更新。

**面板内标记：** 当某个面板的数据触发警告时，该面板的标题右侧显示红色小圆点。点击面板时自动展开。

### Phase 5：视觉统一化

**CSS 优化：**

- 新增 `.dashboard-card` 样式类，移除面板中的内联 style
- 统一面板间距：`margin-top: 24px`（替代混杂的 30px/50px）
- Dashboard 信号圆环：纯 CSS 的 SVG 环形进度条，不依赖 ECharts
- 警示横幅：红色渐变背景 + 左侧闪烁边框 + 自动消失/固定两种模式

---

## 四、文件变更清单

| 文件 | 变更类型 | 内容 |
|:---|:---|:---|
| `core/decision_signal.py` | 🆕 新建 | 5 因子加权评分引擎 |
| `core/alert_state.py` | 🆕 新建 | 全局警示状态管理 |
| `data_engine.py` | ✏ 修改 | 新增 `/api/macro/decision` 路由 |
| `core/quant_engine.py` | ✏ 修改 | 计算后写入 alert_state |
| `core/yield_curve.py` | ✏ 修改 | 倒挂时写入 alert_state |
| `static/index.html` | 🔧 重构 | Hero → Dashboard + 面板折叠分组 |
| `static/main.js` | 🔧 重构 | 新增 initDashboard + 折叠逻辑，精简至 ~500 行 |
| `static/styles.css` | ✏ 修改 | 新增 dashboard 样式类 + 警示动画 |

---

## 五、实施优先级

| 顺序 | 阶段 | 工时 | 交付后效果 |
|:---|:---|---:|:---|
| **1** | 综合信号引擎 + Dashboard | 3h | 首屏显示大号买入/卖出信号 + 仓位建议 + 警示 |
| **2** | 面板折叠分组 | 1h | 页面长度缩减 60%，关键面板默认展开 |
| **3** | 跨面板警示联动 | 2h | 利率倒挂→配置面板标红、压力测试自动展开 |
| **4** | 视觉统一化 | 1.5h | 移除所有内联 style，统一 design token |

**建议一次性执行 Phase 1+2（约 4h），可获得最大的用户体验跃升。**

---

> 编制人：Claude（全系统架构审计）
