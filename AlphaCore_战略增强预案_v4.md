# AlphaCore — 战略增强预案 v4（机构级决策深度）

> 编制日期：2026-05-06  
> 当前版本：v4.0（13 路由 + 9 面板 + Dashboard + 3 层缓存）  
> 审计覆盖：21 个 Python 源文件 + 3 个前端文件（100% 覆盖）

---

## 〇、当前架构全貌

```
                     ┌─────────────────────┐
                     │    Dashboard 首屏     │ ← 信号圆环 + 象限 + 仓位 + 警示
                     └─────────┬───────────┘
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
   │ 📡 宏观雷达   │   │ ⚠️ 风险分析   │   │ 📐 组合优化   │
   │ TNX / VIX    │   │ 相关性矩阵    │   │ 配置沙盘      │
   │ 利率期限结构  │   │ 蒙特卡洛 VaR  │   │ 有效前沿      │
   │              │   │ 历史压力测试  │   │ 18年回测      │
   └──────────────┘   └──────────────┘   └──────────────┘
          │                    │                    │
          └────────────────────┼────────────────────┘
                               ▼
              ┌────────────────────────────┐
              │  数据层 (3-layer cache)     │
              │  L1: 路由结果 / L2: 提供者  │
              │  Tushare QDII + FRED       │
              └────────────────────────────┘
```

**优势：** 美国宏观量化全覆盖、决策信号引擎、生产级缓存、警示联动  
**盲区：** 零中国本土市场深度、零行业/板块分析、零数据导出、零宏观指标面板

---

## 一、对标差距：机构级终端缺失的能力

| 能力维度 | Bloomberg | Wind | 东方财富 | **AlphaCore 当前** |
|:---|:---:|:---:|:---:|:---:|
| 美股宏观量化 | ✅ | ❌ | ❌ | ✅ 全功能 |
| A股市场深度 | ❌ | ✅ | ✅ | ❌ 仅 CSI300 一条线 |
| 申万行业轮动 | ❌ | ✅ | ✅ | ❌ |
| 中国宏观指标 (CPI/PMI/M2) | ❌ | ✅ | ✅ | ❌ |
| 市场宽度 (涨跌比/新高新低) | ✅ | ✅ | ✅ | ❌ |
| 数据导出 (CSV/PDF) | ✅ | ✅ | ✅ | ❌ |
| 警示历史与配置 | ✅ | ✅ | ❌ | ⚠️ 有推送无面板 |
| 盘中实时刷新 | ✅ | ✅ | ✅ | ⚠️ 30min 轮询 |

---

## 二、建议新增：中国本土化 + 机构化增强（5 件套）

### 2.1 申万行业轮动热力图 — 填补最大的市场盲区

**对标：** Wind 行业轮动、东方财富板块排行

**为什么是 No.1：** 系统当前只有一个 CSI300 点位，对 A 股市场的认知相当于"只知道上证指数多少点"。申万 31 个一级行业的轮动是 A 股主动管理的核心决策维度。Tushare 有完整的 `index_classify` + `index_daily` 支持。

**实现：**
- 后端新增 `core/sector_rotation.py`：拉取申万 31 个一级行业指数近 60 日日线，计算各行业相对沪深 300 的超额收益和动量
- 前端新增 ECharts treemap（树状热力图）：每个方块是一个行业，面积=市值权重，颜色=近 20 日涨跌幅（红涨绿跌）
- 下方附带轮动表格：行业名 / 近5日涨跌 / 近20日涨跌 / 近60日涨跌 / 相对强度排名
- 新增路由：`GET /api/macro/sector_rotation`

**工时：** 后端 2h + 前端 2.5h

### 2.2 中国宏观指标面板 — 决策的第二维度

**对标：** Wind EDB、Bloomberg ECST

**为什么是 No.2：** 当前系统只从美国视角判断"全球流动性"。但中国投资者最关心的是国内政策周期——CPI 是否通缩、PMI 是否在荣枯线上、M2 是否加速。这些数据 Tushare 全部免费提供。

**实现：**
- 后端新增 `core/china_macro.py`：通过 Tushare `cn_cpi`、`cn_pmi`、`cn_m`（M2 货币供应量）、`cn_gdp` 拉取近 24 个月数据
- 前端新增"中国宏观"卡片（归入宏观雷达分组）：四条迷你折线图并列（CPI/PMI/M2/GDP 增速），每个带当前值 + 环比变化箭头
- 新增路由：`GET /api/macro/china_macro`

**工时：** 后端 1.5h + 前端 2h

### 2.3 市场宽度仪表 — 看透指数背后的真实温度

**对标：** Bloomberg A/D Line、TradingView Market Breadth

**为什么是 No.3：** 指数涨不代表市场好——如果只有银行股在拉指数、4000 只个股下跌，那指数就是虚假繁荣。市场宽度指标揭示"水面下的真实温度"。

**实现：**
- 后端新增 `core/market_breadth.py`：通过 Tushare `daily_basic` 获取全 A 股涨跌家数，计算腾落线 (AD Line)、新高/新低比率
- 前端嵌入宏观雷达分组底部：一条 AD Line 累计曲线 + 涨跌比柱状图
- 新增路由：`GET /api/macro/market_breadth`

**工时：** 后端 2h（全市场数据量较大）+ 前端 1.5h

### 2.4 数据导出 — 从"看"到"用"的关键一步

**对标：** 所有机构终端标配

**为什么是 No.4：** 投资决策需要记录和分享。当前用户看到回测 8.58% CAGR 的结果只能截图。增加导出可以导出为 CSV 下载到本地做进一步分析，或导出为图片分享。

**实现：**
- 为每条路由增加 `?format=csv` 参数，返回 CSV 格式（不需要额外库）
- 前端每个图表卡片右上角增加"导出 CSV"小按钮
- 前端 Dashboard 增加"导出日报"按钮，生成包含当天所有关键指标的摘要文本

**工时：** 后端 1h + 前端 1h

### 2.5 警示管理中心 — 从被动收到主动管理

**对标：** Bloomberg ALRT、TradingView Alerts

**为什么是 No.5：** 当前警示只有 Dashboard 横幅展示，没有历史记录，无法回溯"上周触发过几次利率倒挂警告"。增加警示日志和阈值配置。

**实现：**
- 后端改造 `core/alert_state.py`：将每次 `set_alert` 写入 SQLite 日志表（含时间戳 + 级别 + 详情）
- 新增 `GET /api/alerts/history?limit=50` + `GET /api/alerts/config`（当前阈值配置）
- 前端新增"警示中心"卡片（归入风险分析分组）：最近 50 条警示的时间线列表，每条可展开详情

**工时：** 后端 1.5h + 前端 1.5h

---

## 三、建议精简/移除

| 项目 | 理由 | 操作 |
|:---|:---|:---|
| `optimize.py` | 独立脚本，已被 portfolio_opt.py 有效前沿功能取代 | 可移入 `tools/` 或删除 |
| `market_data.py` 中 `fetch_yfinance_macro` 旧函数残留 | 已重命名为 `fetch_macro_indicator` | 清理废弃函数 |
| `data_engine.py` decision 路由过长 (80 行) | 逻辑臃肿 | 提取为独立函数 |
| 前端的 `styles.css` 中 `.hero` 等旧样式 | Hero 区域已改为 Dashboard，旧样式不再使用 | 清理 |

---

## 四、实施路线

| 顺序 | 功能 | 工时 | 分组 | 交付效果 |
|:---|:---|---:|:---|:---|
| **1** | 申万行业轮动热力图 | 4.5h | 📡 宏观雷达 | 首块中国本土面板，31 行业 treemap |
| **2** | 中国宏观指标面板 | 3.5h | 📡 宏观雷达 | CPI/PMI/M2/GDP 四联迷你图 |
| **3** | 市场宽度仪表 | 3.5h | 📡 宏观雷达 | AD Line + 涨跌比，揭示真实温度 |
| **4** | 数据导出 | 2h | 全面板 | 每卡片 CSV 导出 + 日报一键生成 |
| **5** | 警示管理中心 | 3h | ⚠️ 风险分析 | 警示历史时间线 + 阈值查看 |
| **6** | 代码清理 | 1h | 架构 | 移除死代码，精简路由 |

**合计：约 17.5h。建议分两轮执行：先做 1+2+3（中国本土化三件套，约 11.5h），再做 4+5+6（机构化打磨，约 6h）。**

---

## 五、完成后的系统全景

```
Dashboard 首屏 → 综合信号 + 当前象限 + 仓位建议
├─ 📡 宏观雷达 (6 panels)
│   ├─ 10Y Treasury (美国)
│   ├─ VIX (美国)
│   ├─ 利率期限结构 (美国)
│   ├─ 🆕 申万行业轮动热力图 (中国)
│   ├─ 🆕 中国宏观指标 CPI/PMI/M2 (中国)
│   └─ 🆕 市场宽度 AD Line (中国)
├─ ⚠️ 风险分析 (4 panels)
│   ├─ 相关性风险矩阵
│   ├─ 蒙特卡洛 VaR
│   ├─ 历史情景压力测试
│   └─ 🆕 警示管理中心
├─ 📐 组合优化 (3 panels)
│   ├─ 资产配置沙盘
│   ├─ Markowitz 有效前沿
│   └─ 18年策略回测
└─ 📤 数据导出 (每面板按钮 + 日报)
```

---

> 编制人：Claude（全量架构审计 + Wind/Bloomberg/东方财富 三标杆对标）
