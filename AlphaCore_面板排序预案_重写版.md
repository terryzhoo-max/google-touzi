# AlphaCore — 面板排序优化预案（重写版）

> 编制日期：2026-05-13  
> 原则：**零破坏、可回退**。每次仅移动一个面板，验证后再移下一个。

---

## 当前布局（工作正常，勿整体重写）

```
Dashboard 首屏
  └─ 信号圆环 + 象限 + 仓位
  └─ 新鲜度指示器
机构决策面板（重，占 40% 首屏）
机构工作台面板
ETF 配置模型面板
全球资产表（独立卡片）
警示横幅
快速导航条
📡 宏观雷达 [展开]
  ├─ TNX
  ├─ VIX  
  ├─ 利率曲线
  ├─ Fed 利率
  ├─ 中国宏观
  ├─ 北向资金
  ├─ 估值温度计
  ├─ 申万行业轮动
  ├─ 主题/ETF 轮动面板
⚠️ 风险分析 [展开]
  ├─ 告警中心
  ├─ 相关性矩阵
  ├─ 蒙特卡洛
  └─ 历史压力测试
📐 组合优化 [折叠]
  ├─ 配置沙盘
  ├─ 有效前沿
  └─ 18年回测
🤖 AI CIO
📖 研报
```

---

## 三刀精准手术（每刀独立可验证）

### 刀 1：全球资产表归入宏观雷达组（5min，零风险）

**操作：** 将 `<div class="global-assets-panel">` 从独立位置剪切，粘贴到 📡 宏观雷达 `<details>` 组内最后一个面板之后、`</details>` 之前。

**验证：** 刷新后全球资产表应在宏观雷达组展开时可见，折叠时隐藏。

### 刀 2：机构三面板加折叠壳（5min，零风险）

**操作：** 在 `<section id="institutional-decision-panel">` 前加 `<details class="panel-group"><summary>🏛 机构决策</summary>`，在 `<section id="allocation-model-panel">` 后加 `</details>`。三个面板的 HTML 内容**一字不改**，仅外层包裹。

**验证：** 刷新后机构面板默认折叠，点击 🏛 展开。所有元素 ID 不变，JS 无需改动。

### 刀 3：机构面板组默认折叠（2min，零风险）

**操作：** 上一步的 `<details>` 不加 `open` 属性，即为默认折叠。

**验证：** 首屏不再显示机构面板，节省 40% 空间。

---

## 做完后的布局

```
Dashboard 首屏
  └─ 信号 + 象限 + 仓位
  └─ 新鲜度 + 警示横幅
  └─ 守卫条（合规/因子/追踪误差）
🏛 机构决策 [折叠]        ← 🆕 三面板入壳
📡 宏观雷达 [展开]
  ├─ TNX / VIX / 利率曲线
  ├─ Fed / 中国宏观 / 北向
  ├─ 估值 / 行业轮动 / 主题ETF
  └─ 🌍 全球资产表        ← 🆕 归入此组
⚠️ 风险分析 [展开]
📐 组合优化 [折叠]
🤖 AI CIO
```

---

## 工时与风险

| 刀 | 工时 | 改动行数 | 风险 |
|:---|:---:|:---:|:---|
| 1. 全球资产归入宏观 | 5min | ~15 行移动 | 无 |
| 2. 机构加折叠壳 | 5min | +2 行标签 | 无 |
| 3. 默认折叠 | 2min | 0 行（不加 open） | 无 |

**总计：12min，仅移动 HTML 标签，不改任何 ID/JS/CSS。**

---

> 原则：**不重写、不替换、不删元素。只做剪切粘贴 + 外层包裹。**
